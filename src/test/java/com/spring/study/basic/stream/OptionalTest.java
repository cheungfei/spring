package com.spring.study.basic.stream;

/**
 * @author zhangfei
 * @version 1.0
 * @date 2021-06-12 19:53
 */
public class OptionalTest {
    public static void main(String[] args) {
//        People people = new People("Kitty",18);
//        People people2 = new People("Google",30);
//        // 将对象people包装
//        Optional<People> optional = Optional.of(people);
//        // 如果Optional非空，对p进行操作，p就是Optional的成员变量value
//        optional.ifPresent( p -> p.setAge(20));
//        System.out.println(optional.flatMap(p -> {
//            p.setName("Jane");
//            // flatMap要返回Optional
//            return Optional.of(p);
//        }).filter(p -> p.getAge() == 18).orElse(people2));
//        optional.ifPresent(System.out::println);
    }
}
